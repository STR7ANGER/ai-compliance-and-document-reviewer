# Human review workflow

Every proposed revision is attached to a tenant-scoped finding and starts as a draft review. The allowed lifecycle is `DRAFT → IN_REVIEW → APPROVED | REJECTED`; rejected work can return to review. Only owners/admins approve. Assignment is explicit, comments retain author/time, and optimistic revisions prevent silent overwrites.
