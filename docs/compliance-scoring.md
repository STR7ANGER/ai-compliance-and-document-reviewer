# Frameworks and findings

Frameworks are versioned per organization and contain weighted controls. Assessments accept only controls from the selected tenant-owned framework and documents from the requested matter. Risk is `severity weight × confidence × control weight`, capped at 100. The dashboard aggregates severity counts and a normalized compliance score while returning evidence-backed findings.
