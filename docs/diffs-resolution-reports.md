# Diffs, resolution, and reports

Version comparison is permitted only when both versions belong to the same tenant-owned document. Findings can be resolved or accepted only by reviewers using evidence chunks from the same matter. Resolution records the actor, timestamp, note, and immutable evidence links. Reports are exported as JSON or escaped CSV and inherit the same tenant boundary.
